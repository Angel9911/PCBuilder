<!-- Modal Container -->
<div id="auth-modal" class="hidden fixed inset-0 bg-black bg-opacity-50 backdrop-blur-sm flex items-center justify-center z-50">
    <div class="bg-white p-6 rounded-lg shadow-lg w-96 relative">
        <button id="close-auth-modal" class="absolute top-3 right-3 text-gray-500 hover:text-gray-800">
            ✖
        </button>

        <!-- Toggle Buttons -->
        <div class="flex justify-center border-b pb-2">
            <button id="show-login" class="tab-btn active text-blue-600 font-semibold mr-4">Log In</button>
            <button id="show-register" class="tab-btn text-gray-500">Sign Up</button>
        </div>

        <!-- Include Login and Register Forms -->
        {% include 'auth/login_form.html.twig' %}
        {% include 'auth/registration_form.html.twig' %}
    </div>
</div>
<!-- End Modal Container -->
