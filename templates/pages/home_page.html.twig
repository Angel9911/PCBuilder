{% extends 'base.html.twig' %}

{% block content %}
    <link rel="stylesheet" href="{{ asset('../css/pc_build_configuration.css') }}">

    <!-- code for spinner -->
    <div id="loading-spinner" class="loading-spinner hidden">
        <div class="spinner"></div>
    </div>
    <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">

        <!-- Features Section -->
        <div id="main-content">
            <h2 class="text-2xl font-bold">Създай своя перфектен компютър</h2>
            <!-- Subtitle -->
            <p class="text-lg text-gray-600 mt-4">
                Нека ти помогнем да създадеш персонален компютър, който напълно отговаря на твоите нужди и бюджет.
            </p>

            <!-- Buttons -->
            <div class="mt-6 flex justify-center space-x-4">
                <!-- Primary Button -->
                <a href="#" id = 'start-config' class="bg-blue-700 text-white px-6 py-3 rounded-md text-lg font-medium hover:bg-blue-800 transition">
                    Започни своята конфигурация
                </a>

                <!-- Secondary Button -->
                <a href="{{ path('configurator.manual') }}" class="border border-gray-400 text-gray-700 px-6 py-3 rounded-md text-lg font-medium hover:bg-gray-100 transition">
                    Преминете към ръчна конфигурация
                </a>
            </div>
            <div class="mt-6 grid grid-cols-1 md:grid-cols-3 gap-6 px-6 max-w-5xl mx-auto">
                <!-- Feature 1 -->
                <div class="bg-white shadow-md p-6 rounded-lg flex flex-col items-center">
                    <span class="text-blue-600 text-3xl">&#10067;</span>
                    <h3 class="font-semibold mt-3">AI препоръки</h3>
                    <p class="text-gray-600 text-sm mt-1">Нашият изкуствен интелект анализира нуждите ви и предлага идеалните компоненти.</p>
                </div>
                <!-- Feature 2 -->
                <div class="bg-white shadow-md p-6 rounded-lg flex flex-col items-center">
                    <span class="text-blue-600 text-3xl">&#128187;</span>
                    <h3 class="font-semibold mt-3">Гарантирана съвместимост</h3>
                    <p class="text-gray-600 text-sm mt-1">Всички компоненти се проверяват за съвместимост, преди да направите покупка.</p>
                </div>
                <!-- Feature 3 -->
                <div class="bg-white shadow-md p-6 rounded-lg flex flex-col items-center">
                    <span class="text-blue-600 text-3xl">&#128722;</span>
                    <h3 class="font-semibold mt-3">Най-добрите цени</h3>
                    <p class="text-gray-600 text-sm mt-1">Сравняваме цените в различни търговци, за да ви предложим най-изгодната оферта.</p>
                </div>
            </div>
        </div>
        <!-- Hidden Questionnaire Section -->
        <div class = "max-w-3xl mx-auto">
            <!-- Questionnaire (Initially Hidden) -->
            <div id="questionnaire" class="hidden bg-white shadow overflow-hidden sm:rounded-lg">
                <div class="px-4 py-5 sm:px-6">
                    <h3 class="text-lg leading-6 font-medium text-gray-900">
                        Step <span id="step-number">1</span> of <span id="total-steps"></span>
                    </h3>
                    <div class="mt-2 flex w-full bg-gray-200 rounded-full h-2.5">
                        <div id="progress-bar" class="bg-blue-600 h-2.5 rounded-full transition-all duration-300" style="width: 25%"></div>
                    </div>
                </div>

                <div class="border-t border-gray-200">
                    <div class="px-4 py-5 sm:p-6">
                        <h3 id="question-title" class="text-xl font-semibold text-gray-900 mb-4"></h3>
                        <div id="options" class="space-y-3"></div>
                    </div>
                </div>

                <div class="px-4 py-4 flex justify-between">
                    <button id="prev-btn" class="hidden px-4 py-2 bg-gray-300 text-gray-700 rounded-md hover:bg-gray-400">
                        ⬅ Back
                    </button>
                    <button id="next-btn" class="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700">
                        Next ➡
                    </button>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
