{% extends 'base.html.twig' %}

{% block title %}PC Configuration{% endblock %}

{% block content %}
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="md:flex md:items-center md:justify-between">
            <div class="flex-1 min-w-0">
                <h2 class="text-2xl font-bold leading-7 text-gray-900 sm:text-3xl sm:truncate">PC Configuration</h2>
                <p class="mt-1 text-sm text-gray-500">Based on your answers, we've pre-selected some components for you.</p>
            </div>
            <div class="mt-4 flex md:mt-0 md:ml-4">
                <button id = 'start-config' class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">Решете отново въпросника</button>
                <button class="ml-3 inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-shopping-cart mr-2 -ml-1 h-5 w-5">
                        <circle cx="8" cy="21" r="1"></circle>
                        <circle cx="19" cy="21" r="1"></circle>
                        <path d="M2.05 2.05h2l2.66 12.42a2 2 0 0 0 2 1.58h9.78a2 2 0 0 0 1.95-1.57l1.65-7.43H5.12"></path>
                    </svg>
                    Add to Cart
                </button>
            </div>
        </div>
        <div class="mt-8 grid grid-cols-1 gap-6 lg:grid-cols-3">
            <!-- Component selection -->
            <div class="lg:col-span-2">
                <div class="bg-white shadow overflow-hidden sm:rounded-lg">
                    <ul class="divide-y divide-gray-200">

                        <li class="group hover:bg-gray-50 transition-colors duration-150">
                            <div class="px-4 py-4 sm:px-6">
                                <!-- Component Header -->
                                <div class="flex items-center justify-between mb-4">
                                    <div class="flex items-center">
                                        <!-- CPU Icon -->
                                        <div class="flex-shrink-0 bg-blue-100 rounded-md p-2">
                                            <img src="{{ asset('/assets/images/icons8-processor-50.png') }}" alt="CPU" class="w-9 h-9">
                                        </div>
                                        <div class="ml-4">
                                            <h4 class="text-lg font-medium text-gray-900">Processor (CPU)</h4>
                                        </div>
                                    </div>
                                    <button class="text-sm text-blue-600 hover:text-blue-800 font-medium flex items-center" onclick="window.location.href='/components/cpu/filter'">
                                        Advanced Filter
                                        <i data-lucide="Filter" class="inline-block h-4 w-4 ml-1"></i>
                                    </button>
                                </div>

                                <!-- Component Selection and Show Offers -->
                                <div class="flex items-center space -x-4">
                                    <div class="relative flex-1">
                                        <select id = "cpu-select" data-component-id="cpu" class="block w-full pl-3 pr-10 py-3 text-base border-gray-300 focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm rounded-lg transition-shadow duration-150 hover:shadow-md group-hover:border-blue-300">
                                            <option value="">Select Processor (CPU)</option>
                                            {% for cpu in cpus %}
                                                <option value="{{ cpu.id }}">{{ cpu.name }}</option>
                                            {% endfor %}
                                        </select>
                                        <!-- Spinner -->
                                        <div data-spinner-for="cpu" class="loading-spinner hidden"></div>
                                        <div class="absolute inset-y-0 right-0 flex items-center pr-2 pointer-events-none">
                                            <i data-lucide="ChevronDown" class="h-5 w-5 text-gray-400"></i>
                                        </div>
                                    </div>

                                    <!-- Hide Offers Button -->
                                    <!-- Include the Hide Offers button -->
                                    {% include 'pages/pages_templates/hide-offer-template.html.twig' with { component_id: 'cpu' } %}

                                </div>

                                <!-- Offers Section -->
                                <!-- Hidden template for vendor offers -->
                                <div class="mt-4 space-y-3 vendor-offers hidden" data-offers-for="cpu"></div>

                            </div>
                        </li>

                        <li class="group hover:bg-gray-50 transition-colors duration-150">
                            <div class="px-4 py-4 sm:px-6">
                                <!-- Component Header -->
                                <div class="flex items-center justify-between mb-4">
                                    <div class="flex items-center">
                                        <div class="flex-shrink-0 bg-blue-100 rounded-md p-2">
                                            <img src="{{ asset('/assets/images/icons8-burn-cd-50.png') }}" alt="CPU Cooling" class="w-9 h-9">
                                        </div>
                                        <div class="ml-4">
                                            <h4 class="text-lg font-medium text-gray-900">CPU Cooling</h4>
                                        </div>
                                    </div>
                                    <button class="text-sm text-blue-600 hover:text-blue-800 font-medium flex items-center" onclick="window.location.href='/components/cooling/filter'">
                                        Advanced Filter
                                        <i data-lucide="Filter" class="inline-block h-4 w-4 ml-1"></i>
                                    </button>
                                </div>

                                <!-- Component Selection and Show Offers -->
                                <div class="flex items-center space-x-4">
                                    <div class="relative flex-1">
                                        <select id = "cpu_cooling" data-component-id="cpu_cooling" class="block w-full pl-3 pr-10 py-3 text-base border-gray-300 focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm rounded-lg transition-shadow duration-150 hover:shadow-md group-hover:border-blue-300">
                                            <option value="">Select CPU Cooling</option>

                                            <option value="Cooler Master Hyper 212">
                                                Cooler Master Hyper 212
                                            </option>

                                            <option value="NZXT Kraken X63">
                                                NZXT Kraken X63
                                            </option>

                                            <option value="Noctua NH-D15">
                                                Noctua NH-D15
                                            </option>

                                        </select>
                                        <div class="absolute inset-y-0 right-0 flex items-center pr-2 pointer-events-none">
                                            <i data-lucide="ChevronDown" class="h-5 w-5 text-gray-400"></i>
                                        </div>
                                    </div>
                                    <!-- Hide Offers Button -->
                                    <!-- Include the Hide Offers button -->
                                    {% include 'pages/pages_templates/hide-offer-template.html.twig' with { component_id: 'cooling' } %}
                                </div>

                                <!-- Offers Section -->
                                <!-- Hidden template for vendor offers -->
                                <div class="mt-4 space-y-3 vendor-offers hidden" data-offers-for="cooling"></div>
                            </div>
                        </li>

                        <li class="group hover:bg-gray-50 transition-colors duration-150">
                            <div class="px-4 py-4 sm:px-6">
                                <!-- Component Header -->
                                <div class="flex items-center justify-between mb-4">
                                    <div class="flex items-center">
                                        <div class="flex-shrink-0 bg-blue-100 rounded-md p-2">
                                            <img src="{{ asset('/assets/images/icons8-motherboard-50.png') }}" alt="Motherboard" class="w-9 h-9">
                                        </div>
                                        <div class="ml-4">
                                            <h4 class="text-lg font-medium text-gray-900">Motherboard</h4>
                                        </div>
                                    </div>
                                    <button class="text-sm text-blue-600 hover:text-blue-800 font-medium flex items-center" onclick="window.location.href='/components/motherboard/filter'">
                                        Advanced Filter
                                        <i data-lucide="Filter" class="inline-block h-4 w-4 ml-1"></i>
                                    </button>
                                </div>

                                <!-- Component Selection and Show Offers -->
                                <div class="flex items-center space-x-4">
                                    <div class="relative flex-1">
                                        <select id = "motherboard" data-component-id="motherboard" class="block w-full pl-3 pr-10 py-3 text-base border-gray-300 focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm rounded-lg transition-shadow duration-150 hover:shadow-md group-hover:border-blue-300">
                                            <option value="">Select Motherboard</option>
                                            {% for motherboard in motherboards %}
                                                <option value="{{ motherboard.id }}">{{ motherboard.name }}</option>
                                            {% endfor %}
                                        </select>
                                        <div class="absolute inset-y-0 right-0 flex items-center pr-2 pointer-events-none">
                                            <i data-lucide="ChevronDown" class="h-5 w-5 text-gray-400"></i>
                                        </div>
                                    </div>
                                    <!-- Hide Offers Button -->
                                    <!-- Include the Hide Offers button -->
                                    {% include 'pages/pages_templates/hide-offer-template.html.twig' with { component_id: 'motherboard' } %}
                                </div>

                                <!-- Offers Section -->
                                <!-- Hidden template for vendor offers -->
                                <div class="mt-4 space-y-3 vendor-offers hidden" data-offers-for="motherboard"></div>

                            </div>
                        </li>

                        <li class="group hover:bg-gray-50 transition-colors duration-150">
                            <div class="px-4 py-4 sm:px-6">
                                <!-- Component Header -->
                                <div class="flex items-center justify-between mb-4">
                                    <div class="flex items-center">
                                        <div class="flex-shrink-0 bg-blue-100 rounded-md p-2">
                                            <img src="{{ asset('/assets/images/icons8-memory-slot-50.png') }}" alt="Memory" class="w-9 h-9">
                                        </div>
                                        <div class="ml-4">
                                            <h4 class="text-lg font-medium text-gray-900">Memory (RAM)</h4>
                                        </div>
                                    </div>
                                    <button class="text-sm text-blue-600 hover:text-blue-800 font-medium flex items-center" onclick="window.location.href='/components/ram/filter'">
                                        Advanced Filter
                                        <i data-lucide="Filter" class="inline-block h-4 w-4 ml-1"></i>
                                    </button>
                                </div>

                                <!-- Component Selection and Show Offers -->
                                <div class="flex items-center space-x-4">
                                    <div class="relative flex-1">
                                        <select id = "ram" data-component-id="ram" class="block w-full pl-3 pr-10 py-3 text-base border-gray-300 focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm rounded-lg transition-shadow duration-150 hover:shadow-md group-hover:border-blue-300">
                                            <option value="">Select Memory(RAM)</option>
                                            {% for ram in rams %}
                                                <option value="{{ ram.id }}">{{ ram.name }}</option>
                                            {% endfor %}
                                        </select>
                                        <div class="absolute inset-y-0 right-0 flex items-center pr-2 pointer-events-none">
                                            <i data-lucide="ChevronDown" class="h-5 w-5 text-gray-400"></i>
                                        </div>
                                    </div>
                                    <!-- Hide Offers Button -->
                                    <!-- Include the Hide Offers button -->
                                    {% include 'pages/pages_templates/hide-offer-template.html.twig' with { component_id: 'ram' } %}
                                </div>

                                <!-- Offers Section -->
                                <!-- Hidden template for vendor offers -->
                                <div class="mt-4 space-y-3 vendor-offers hidden" data-offers-for="ram"></div>

                            </div>
                        </li>

                        <li class="group hover:bg-gray-50 transition-colors duration-150">
                            <div class="px-4 py-4 sm:px-6">
                                <!-- Component Header -->
                                <div class="flex items-center justify-between mb-4">
                                    <div class="flex items-center">
                                        <div class="flex-shrink-0 bg-blue-100 rounded-md p-2">
                                            <img src="{{ asset('/assets/images/icons8-video-card-50.png') }}" alt="Memory" class="w-9 h-9">
                                        </div>
                                        <div class="ml-4">
                                            <h4 class="text-lg font-medium text-gray-900">Video Card</h4>
                                        </div>
                                    </div>
                                    <button class="text-sm text-blue-600 hover:text-blue-800 font-medium flex items-center" onclick="window.location.href='/components/gpu/filter'">
                                        Advanced Filter
                                        <i data-lucide="Filter" class="inline-block h-4 w-4 ml-1"></i>
                                    </button>
                                </div>


                                <div class="flex items-center space-x-4">
                                    <div class="relative flex-1">
                                        <select id = "gpu" data-component-id="gpu" class="block w-full pl-3 pr-10 py-3 text-base border-gray-300 focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm rounded-lg transition-shadow duration-150 hover:shadow-md group-hover:border-blue-300">
                                            <option value="">Select Video Card</option>
                                            {% for gpu in gpus %}
                                                <option value="{{ gpu.id }}">{{ gpu.name }}</option>
                                            {% endfor %}
                                        </select>
                                        <div class="absolute inset-y-0 right-0 flex items-center pr-2 pointer-events-none">
                                            <i data-lucide="ChevronDown" class="h-5 w-5 text-gray-400"></i>
                                        </div>
                                    </div>
                                    <!-- Hide Offers Button -->
                                    <!-- Include the Hide Offers button -->
                                    {% include 'pages/pages_templates/hide-offer-template.html.twig' with { component_id: 'gpu' } %}
                                </div>

                                <!-- Offers Section -->
                                <!-- Hidden template for vendor offers -->
                                <div class="mt-4 space-y-3 vendor-offers hidden" data-offers-for="gpu"></div>

                            </div>
                        </li>

                        <li class="group hover:bg-gray-50 transition-colors duration-150">
                            <div class="px-4 py-4 sm:px-6">
                                <!-- Component Header -->
                                <div class="flex items-center justify-between mb-4">
                                    <div class="flex items-center">
                                        <div class="flex-shrink-0 bg-blue-100 rounded-md p-2">
                                            <img src="{{ asset('/assets/images/icons8-ssd-50.png') }}" alt="Memory" class="w-9 h-9">
                                        </div>
                                        <div class="ml-4">
                                            <h4 class="text-lg font-medium text-gray-900">Storage</h4>
                                        </div>
                                    </div>
                                    <button class="text-sm text-blue-600 hover:text-blue-800 font-medium flex items-center" onclick="window.location.href='/components/storage/filter'">
                                        Advanced Filter
                                        <i data-lucide="Filter" class="inline-block h-4 w-4 ml-1"></i>
                                    </button>
                                </div>


                                <div class="flex items-center space-x-4">
                                    <div class="relative flex-1">
                                        <select id="storage" data-component-id="storage" class="block w-full pl-3 pr-10 py-3 text-base border-gray-300 focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm rounded-lg transition-shadow duration-150 hover:shadow-md group-hover:border-blue-300">
                                            <option value="">Select Storage</option>

                                            {% for storage in storages %}
                                                <option value="{{ storage.id }}">{{ storage.name }}</option>
                                            {% endfor %}

                                        </select>
                                        <div class="absolute inset-y-0 right-0 flex items-center pr-2 pointer-events-none">
                                            <i data-lucide="ChevronDown" class="h-5 w-5 text-gray-400"></i>
                                        </div>
                                    </div>
                                    <!-- Hide Offers Button -->
                                    <!-- Include the Hide Offers button -->
                                    {% include 'pages/pages_templates/hide-offer-template.html.twig' with { component_id: 'storage' } %}
                                </div>

                                <!-- Offers Section -->
                                <!-- Hidden template for vendor offers -->
                                <div class="mt-4 space-y-3 vendor-offers hidden" data-offers-for="storage"></div>

                            </div>
                        </li>

                        <li class="group hover:bg-gray-50 transition-colors duration-150">
                            <div class="px-4 py-4 sm:px-6">
                                <!-- Component Header -->
                                <div class="flex items-center justify-between mb-4">
                                    <div class="flex items-center">
                                        <div class="flex-shrink-0 bg-blue-100 rounded-md p-2">
                                            <img src="{{ asset('/assets/images/icons8-power-over-ethernet-50.png') }}" alt="Memory" class="w-9 h-9">
                                        </div>
                                        <div class="ml-4">
                                            <h4 class="text-lg font-medium text-gray-900">Power Supply</h4>
                                        </div>
                                    </div>
                                    <button class="text-sm text-blue-600 hover:text-blue-800 font-medium flex items-center" onclick="window.location.href='/components/psu/filter'">
                                        Advanced Filter
                                        <i data-lucide="Filter" class="inline-block h-4 w-4 ml-1"></i>
                                    </button>
                                </div>

                                <!-- Component Selection and Show Offers -->
                                <div class="flex items-center space-x-4">
                                    <div class="relative flex-1">
                                        <select id = "psu" data-component-id="psu" class="block w-full pl-3 pr-10 py-3 text-base border-gray-300 focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm rounded-lg transition-shadow duration-150 hover:shadow-md group-hover:border-blue-300">
                                            <option value="">Select Power Supply (PSU)</option>
                                            {% for psu in psus %}
                                                <option value="{{ psu.id }}">{{ psu.name }}</option>
                                            {% endfor %}
                                        </select>
                                        <div class="absolute inset-y-0 right-0 flex items-center pr-2 pointer-events-none">
                                            <i data-lucide="ChevronDown" class="h-5 w-5 text-gray-400"></i>
                                        </div>
                                    </div>
                                    <!-- Hide Offers Button -->
                                    <!-- Include the Hide Offers button -->
                                    {% include 'pages/pages_templates/hide-offer-template.html.twig' with { component_id: 'psu' } %}
                                </div>

                                <!-- Offers Section -->
                                <!-- Hidden template for vendor offers -->
                                <div class="mt-4 space-y-3 vendor-offers hidden" data-offers-for="psu"></div>

                            </div>
                        </li>

                        <li class="group hover:bg-gray-50 transition-colors duration-150">
                            <div class="px-4 py-4 sm:px-6">
                                <!-- Component Header -->
                                <div class="flex items-center justify-between mb-4">
                                    <div class="flex items-center">
                                        <div class="flex-shrink-0 bg-blue-100 rounded-md p-2">
                                            <img src="{{ asset('/assets/images/icons8-server-50.png') }}" alt="PC Case" class="w-9 h-9">
                                        </div>
                                        <div class="ml-4">
                                            <h4 class="text-lg font-medium text-gray-900">Case</h4>
                                        </div>
                                    </div>
                                    <button class="text-sm text-blue-600 hover:text-blue-800 font-medium flex items-center" onclick="window.location.href='/components/case/filter'">
                                        Advanced Filter
                                        <i data-lucide="Filter" class="inline-block h-4 w-4 ml-1"></i>
                                    </button>
                                </div>

                                <!-- Component Selection and Show Offers -->
                                <div class="flex items-center space-x-4">
                                    <div class="relative flex-1">
                                        <select id = "pc_case" data-component-id="pc_case" class="block w-full pl-3 pr-10 py-3 text-base border-gray-300 focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm rounded-lg transition-shadow duration-150 hover:shadow-md group-hover:border-blue-300">
                                            <option value="">Select Case</option>

                                            {% for pc_case in pc_cases %}
                                                <option value="{{ pc_case.id }}">{{ pc_case.name }}</option>
                                            {% endfor %}

                                        </select>
                                        <div class="absolute inset-y-0 right-0 flex items-center pr-2 pointer-events-none">
                                            <i data-lucide="ChevronDown" class="h-5 w-5 text-gray-400"></i>
                                        </div>
                                    </div>
                                    <!-- Hide Offers Button -->
                                    <!-- Include the Hide Offers button -->
                                    {% include 'pages/pages_templates/hide-offer-template.html.twig' with { component_id: 'case' } %}
                                </div>

                                <!-- Offers Section -->
                                <!-- Hidden template for vendor offers -->
                                <div class="mt-4 space-y-3 vendor-offers hidden" data-offers-for="case"></div>

                            </div>
                        </li>

                        <li class="group hover:bg-gray-50 transition-colors duration-150">
                            <div class="px-4 py-4 sm:px-6">
                                <!-- Component Header -->
                                <div class="flex items-center justify-between mb-4">
                                    <div class="flex items-center">
                                        <div class="flex-shrink-0 bg-blue-100 rounded-md p-2">
                                            <img src="{{ asset('/assets/images/icons8-monitor-50.png') }}" alt="Monitor" class="w-9 h-9">
                                        </div>
                                        <div class="ml-4">
                                            <h4 class="text-lg font-medium text-gray-900">Monitor</h4>
                                        </div>
                                    </div>
                                    <button class="text-sm text-blue-600 hover:text-blue-800 font-medium flex items-center" onclick="window.location.href='/components/monitor/filter'">
                                        Advanced Filter
                                        <i data-lucide="Filter" class="inline-block h-4 w-4 ml-1"></i>
                                    </button>
                                </div>


                                <div class="flex items-center space-x-4">
                                    <div class="relative flex-1">
                                        <select data-component-id="monitor" class="block w-full pl-3 pr-10 py-3 text-base border-gray-300 focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm rounded-lg transition-shadow duration-150 hover:shadow-md group-hover:border-blue-300">
                                            <option value="">Select Monitor</option>

                                            <option value="LG 27GL83A-B 27" 1440p=" ">
                                            LG 27GL83A-B 27" 1440p 144Hz
                                            </option>

                                            <option value="ASUS TUF Gaming VG259QM 24.5" 1080p="" >
                                            ASUS TUF Gaming VG259QM 24.5" 1080p 280Hz
                                            </option>

                                            <option value="Dell S2721DGF 27" 1440p="" >
                                            Dell S2721DGF 27" 1440p 165Hz
                                            </option>

                                        </select>
                                        <div class="absolute inset-y-0 right-0 flex items-center pr-2 pointer-events-none">
                                            <i data-lucide="ChevronDown" class="h-5 w-5 text-gray-400"></i>
                                        </div>
                                    </div>
                                    <!-- Hide Offers Button -->
                                    <!-- Include the Hide Offers button -->
                                    {% include 'pages/pages_templates/hide-offer-template.html.twig' with { component_id: 'monitor' } %}
                                </div>

                                <!-- Offers Section -->

                            </div>
                        </li>

                    </ul>
                    <div class="px-6 py-4 bg-gray-50 border-t border-gray-200">
                        <button id="saveConfiguration" class="w-full flex justify-center items-center px-6 py-3 border border-transparent text-base font-medium rounded-md text-white bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 shadow-lg transform transition-all duration-200 hover:scale-[1.02] active:scale-[0.98]">
                            <i data-lucide="Save" class="h-5 w-5 mr-2"></i>
                            Запаси Конфигурация
                        </button>
                        <p class="mt-2 text-sm text-center text-gray-500">
                            Запазете конфигурацията си за достъп до нея по-късно или я споделете с други
                        </p>
                    </div>
                </div>
            </div>

            <!-- Summary and recommendations -->
            <div class="lg:col-span-1">
                <div class="bg-white shadow overflow-hidden sm:rounded-lg sticky top-6">
                    <div class="px-4 py-5 sm:px-6">
                        <h3 class="text-lg leading-6 font-medium text-gray-900">
                            Build Summary
                        </h3>
                    </div>
                    <div class="border-t border-gray-200 px-4 py-5 sm:p-0">
                        <dl class="sm:divide-y sm:divide-gray-200">
                            <div class="py-4 sm:py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
                                <dt class="text-sm font-medium text-gray-500">
                                    Selected Components
                                </dt>
                                <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2">
                                    {{ selected_components_count }} of {{ max_components_count }}
                                </dd>
                            </div>
                            <div class="py-4 sm:py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
                                <dt class="text-sm font-medium text-gray-500">
                                    Estimated Total
                                </dt>
                                <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2">
                                    $0.00
                                </dd>
                            </div>
                        </dl>
                    </div>

                    <div class="py-4 sm:py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
                        <dt class="text-sm font-medium text-gray-500">AI Recommendation</dt>
                        <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2">
                            {% if explanation is not empty %}
                                {{ explanation }}
                            {% else %}

                            {% endif %}
                        </dd>
                    </div>

                    <div class="border-t border-gray-200 px-4 py-4 sm:px-6">
                        <button id="addToCartBottom" class="w-full inline-flex justify-center items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                            <i data-lucide="shopping-cart" class="mr-2 -ml-1 h-5 w-5"></i>
                            Add to Cart
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Save Configuration Modal -->
    <div id="saveConfigModal" class="fixed inset-0 flex items-center justify-center bg-gray-900 bg-opacity-50 hidden">
        <div class="bg-white rounded-lg shadow-lg p-6 w-96">
            <h2 class="text-xl font-semibold text-gray-900 mb-4">Въведете име на конфигурацията</h2>
            <input type="text" id="configName" class="w-full px-3 py-2 border rounded-md" placeholder="Моята конфигурация">
            <div class="flex justify-end space-x-2 mt-4">
                <button id="cancelSave" class="px-4 py-2 bg-gray-300 text-gray-700 rounded-md">Отказ</button>
                <button id="confirmSave" class="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">Запази</button>
            </div>
        </div>
    </div>
    <script>
        var pcConfiguration = {{ pcConfiguration|json_encode|raw }};
        var isAiConfiguration = {{ isAiConfiguration|json_encode|raw }};
    </script>
{% endblock %}

